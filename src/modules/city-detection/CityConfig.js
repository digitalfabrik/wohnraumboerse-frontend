class CityConfig {
  constructor (cmsName, hostnames, formsEnabled, title, logo, impressumUrl) {
    this.cmsName = cmsName
    this.hostNames = hostnames
    this.formsEnabled = formsEnabled
    this.title = title
    this.logo = logo
    this.impressumUrl = impressumUrl
  }
}

export default CityConfig
